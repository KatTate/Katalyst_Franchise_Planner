name,displayName,description,module,path,standalone
# This file is intentionally empty. Tool manifests track IDE-specific integrations
# (e.g., Cursor slash commands, Claude Code commands) which are not used in Replit.
# The Replit version uses natural language triggers via replit-routing.md instead.
